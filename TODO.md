# Occupancy by Time — Pending Adjustments

## 1. Timestamp alignment
Right-align the showtime label text (e.g. "10:10") within its cell so it hugs the right wall.

## 2. Occupancy value alignment
Left-align the seats-sold number within its cell so it hugs the left wall.

## 3. Spacer column between hour groups
Insert one narrow empty column between each hour column-pair (e.g. between the 10am pair and the 11am pair) to visually separate the groups.

## 4. Print header
Add a centered print header (Excel page header) reading **"Occupancy by Time+"** so it appears on every printed page of the report.

## 5. Print footer
Add a print footer that shows the date/time range the report is based on (i.e. the filtered start date and end date used to generate the report).
